name: Quality

on:
  push:
    branches: [ main, master, refactor/* ]
  pull_request:
    branches: [ main, master ]

env:
  CARGO_TERM_COLOR: always

jobs:
  format:
    name: Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      
      - name: Check formatting
        run: cargo fmt --all -- --check

  lint:
    name: Clippy Lints
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      
      - name: Stub private PureReason deps (not available in CI)
        run: |
          mkdir -p ../../AI/PureReason/crates
          cp -r stubs/pure-reason-core ../../AI/PureReason/crates/
          cp -r stubs/pure-reason-verifier ../../AI/PureReason/crates/

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
      
      - name: Cache cargo index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}
      
      - name: Cache target directory
        uses: actions/cache@v4
        with:
          path: target
          key: ${{ runner.os }}-target-${{ hashFiles('**/Cargo.lock') }}
      
      - name: Run clippy
        # -D warnings enforces all deny/warn lints declared in Cargo.toml.
        # Run without default features to avoid requiring ONNX model download in CI.
        # The embed+rerank features are covered by the release build check.
        run: cargo clippy --all-targets --no-default-features -- -D warnings

  test:
    name: Test Suite
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Stub private PureReason deps (not available in CI)
        run: |
          mkdir -p ../../AI/PureReason/crates
          cp -r stubs/pure-reason-core ../../AI/PureReason/crates/
          cp -r stubs/pure-reason-verifier ../../AI/PureReason/crates/

      - name: Install system dependencies
        run: sudo apt-get update -q && sudo apt-get install -y libblas-dev liblapack-dev libopenblas-dev gfortran
      
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
      
      - name: Install cargo-nextest
        run: cargo install cargo-nextest --locked

      - name: Run tests
        # --no-default-features skips embed/rerank (require downloaded models + BLAS).
        # Core logic is fully covered; embed path is exercised by the Build Check job.
        run: cargo nextest run --no-default-features
      
      - name: Run doc tests
        run: cargo test --doc --no-default-features

  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install system dependencies
        run: sudo apt-get update -q && sudo apt-get install -y libblas-dev liblapack-dev libopenblas-dev gfortran
      
      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov
      
      - name: Generate coverage
        run: cargo llvm-cov --all-features --lcov --output-path lcov.info
      
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          files: lcov.info
          fail_ci_if_error: false

  build:
    name: Build Check
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install system dependencies (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get update -q && sudo apt-get install -y libblas-dev liblapack-dev libopenblas-dev gfortran
      
      - name: Build
        run: cargo build --release --all-features
      
      - name: Check binary size (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          SIZE=$(stat -c%s target/release/cortyx)
          echo "Binary size: $SIZE bytes"
          # opt-level=3 + lto=true + strip="symbols" produces ~15-20MB on Linux.
          # 25MB limit gives headroom for new features without silently bloating.
          if [ $SIZE -gt 26214400 ]; then
            echo "Binary size exceeds 25MB limit!"
            exit 1
          fi
      
      - name: Check binary size (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          SIZE=$(stat -f%z target/release/cortyx)
          echo "Binary size: $SIZE bytes"
          if [ $SIZE -gt 26214400 ]; then
            echo "Binary size exceeds 25MB limit!"
            exit 1
          fi
